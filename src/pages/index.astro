---
import Layout from "../components/layout.astro";
import PostListing from "../components/post/post-list.astro";
import SlugList from "../components/slug-list.astro";
import { posts, tags } from "../helpers";

const englishPosts = posts.filter(post => !post.data.tags?.includes("Français"));
---

<Layout>
  <SlugList slugs={tags} generateLink={(slug) => `/tags/${encodeURI(slug)}`} />
  <PostListing posts={englishPosts} />
</Layout>
